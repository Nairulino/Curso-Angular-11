import { TruncarDirective } from './truncar.directive';

describe('TruncarDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncarDirective();
    expect(directive).toBeTruthy();
  });
});
